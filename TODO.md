# TODO - Workspace Problems

- [x] Fix entrypoint view: update `app/Controllers/Home.php` so `/` renders `login` (view `index` not found)
- [x] Update CSS only: modify `public/css/style.css` to be more Bootstrap 5-native while keeping login/dashboard structure intact
- [ ] Quick manual test: open `/` then ensure login renders and dashboard switchTab JS still works
- [x] Sesuaikan ukuran logo login dan arahkan tombol masuk ke `dashboard.php`
- [x] Kecilkan logo login dan posisikan lebih ke atas
- [x] Integrasi koneksi DB untuk login (email+password) dan verifikasi password






